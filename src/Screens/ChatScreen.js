import React from 'react';
import {TextInput, View, Image, TouchableOpacity, Text} from 'react-native';

const ChatScreen = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{fontSize: 18}}>{'chatscreen'}</Text>
    </View>
  );
};

export default ChatScreen;
