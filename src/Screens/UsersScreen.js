import React from 'react';
import {FlatList, Text, TouchableOpacity, View} from 'react-native';

const UsersScreen = ({navigation}) => {
  return (
    <View>
      <Text style={{fontSize: 18}}>{'userscreen'}</Text>
    </View>
  );
};

export default UsersScreen;
